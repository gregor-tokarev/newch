import { en as layout } from '~/lang/layout'
import { en as indexPage } from '~/lang/indexPage'
import { en as actions } from '~/lang/actions'
import { en as form } from '~/lang/form'
import { en as elements } from '~/lang/elements'

export default {
  layout,
  indexPage,
  actions,
  form,
  elements
}
